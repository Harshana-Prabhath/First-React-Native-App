import { StyleSheet } from 'react-native';

export const textInputStyles = StyleSheet.create({
  viewStyles: {
    marginTop: 50,
    height: 200,
  },
  inputStyles: {
    height: 40,
    borderWidth: 1,
    marginHorizontal: 10,
  },
});
